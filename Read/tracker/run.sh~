1#!/bin/bash

for file in `ls /unix/muons/g-2/omegaEU/Tracking/gm2*root`; do

    echo $file
    # ignore truncated file 
#    [[ $file == *30.root ]] && continue ; 

    ./plotter_slices $file

done

hadd merged_tracker_slices.root plots*.root
